#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define endl "\n"   // optional, better to avoid redefining endl

void solve() {
    int n,k;
    cin>>n>>k;
    vector<int>arr(n);
    for(int i = 0; i < n; i++)
    {
        cin>>arr[i];
    }

    //ds 
    long long sum = 0;
    
    long long ans = 0;

    int tail = 0;
    int head = -1;

    while(tail < n)
    {
    
        while(head + 1 < n && (sum + arr[head+1] <= k))
        {
            head++;
            sum += arr[head];

        }
        
        //here sum == k so update answer
        ans += head - tail + 1;
        

        //now you have updated your anser and it has size of k so move tail from left
        //so tail++

        if(tail <= head)
        {
            sum -= arr[tail];
            tail++;
        }
        else
        {
            tail++;
            head = tail - 1;
        }
    }

    cout<<ans<<endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}
