//bruteofrce
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define endl "\n"   // optional, better to avoid redefining endl

void solve() {
   
    string s;
    cin>>s;

    int ans = 0;
    char ch = '\0';

    for(int i = 0; i < s.size(); i++)
    {
        int cnt = 1;
        for(int j = i + 1; j < s.size(); j++)
        {
            if(s[i] == s[j])
            {
                cnt++;

            }
            // else
            // {
            //     break;
            // }
        }

        if(cnt > ans)
        {
            ans = cnt;
            ch = s[i];
        }
    }
    cout<<ch<<endl;

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
   cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}


//optimal
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define endl "\n"   // optional, better to avoid redefining endl

void solve() {
   
    string s;
    cin>>s;

    int ans = 1;
    char ch = s[0];
    int cnt = 1;
    for(int i = 1; i < s.size(); i++)
    {
        if(s[i] == s[i-1])
        {
            cnt++;
        }
        else
        {
            cnt = 1;
        }

        if(cnt > ans)
        {
            ans = cnt;
            ch = s[i];
        }
        else if (cnt == ans && s[i] < ch)
{
    ch = s[i];
}
    }
    cout<<ch<<endl;

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
   cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}
