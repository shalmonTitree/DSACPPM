class Solution {
public:
    bool isPalindrome(string s) {
        int l = 0;
        int e = s.size() - 1;
        while(l < e)
        {
            while(l < e && !isalnum(s[l]))l++;
            while(l < e && !isalnum(s[e]))e--;
            if(tolower(s[l]) != tolower(s[e]))
            {
                return false;
            }
            l++;
            e--;
        }
        return true;
    }
};
