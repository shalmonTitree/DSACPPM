```text
Time Limit: 1 sec
Memory: 256 MB

Description

You are given an integer N and a decimal number M. M can be represented as Y.Z, where the dot is the decimal point, and N is referred to as X. You have to find the value (Z + X).(Y + X).

Input Format

The first line of the input contains one integer T — the number of test cases. Then T test cases follow.

The first line of each test case contains one integer N and a decimal number M.

Output Format

For each test case, print the value of (Z + X).(Y + X).

Constraints

1 ≤ T ≤ 10^6

1 ≤ N, M ≤ 10^9

Sample Input 1

3
14 1.2
3 4.5
31 0.143

Sample Output 1

16.15
8.7
174.31

Note

For the first test case, X = 14, Y = 1, Z = 2. The answer is:

(2 + 14).(1 + 14) = 16.15

For the second test case, X = 3, Y = 4, Z = 5. The answer is:

(5 + 3).(4 + 3) = 8.7

For the third test case, X = 31, Y = 0, Z = 143. The answer is:

(143 + 31).(0 + 31) = 174.31
```



#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define endl "\n"   // optional, better to avoid redefining endl

void solve() {
    int n;
    cin>>n;
    string m;
    cin>>m;
    string sy = m.substr(0,m.find('.'));
    string sz = m.substr(m.find('.') + 1);
    int y = stoi(sy);
    int z = stoi(sz);

    
    cout<<(z + n)<<"."<<(y + n)<<endl;


}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}
